<h1>
<p align="center">
<a href="https://github.com/GnomeShift/TiSk" target="_blank" rel="noopener noreferrer">TiSk - Ticket Desk</a>
</p>
</h1>

<p align="center">
  <a href="README.md">🇷🇺 Русский</a>
</p>

## 📋 Содержание
* [🌐Обзор](#-обзор)
    * [⚡Функциональные возможности](#-функциональные-возможности)
* [📥Установка](#-установка)
* [Сборка из исходного кода](#-сборка-из-исходного-кода)
* [⚙️Конфигурация](#-конфигурация)
* [👁️Демо-режим](#-демо-режим)

# 🌐 Обзор
**TiSk** - это тикет-система, позволяющая гибко управлять заявками на обслуживание.

## ⚡ Функциональные возможности
* Управление заявками.
* Управление пользователями
* Демо-режим.
* REST API.
* Предустановленные роли (USER, SUPPORT, ADMIN).
* Модульная архитектура.
* JWT-аутентификация.
* Защищенные эндпоинты.
* Валидация на фронтенде и бэкенде.
* Хранение секретов в `.env`.

# 📥 Установка
#### 1️⃣ Установите необходимые пакеты:
```bash
apt install curl wget docker docker-compose
```

#### 2️⃣ Создайте папку проекта:
```bash
mkdir /opt/tisk && cd /opt/tisk
```

#### 3️⃣ Загрузите `docker-compose.yml`:
```bash
wget https://raw.githubusercontent.com/GnomeShift/TiSk/refs/heads/master/docker-compose.yml
```

#### 4️⃣ Загрузите `.env`:
```bash
wget https://github.com/GnomeShift/TiSk/blob/master/.env.production
```

#### 5️⃣ Загрузите образы:
```bash
docker pull gnomeshift/tisk-backend:latest && docker pull gnomeshift/tisk-frontend:latest
```

# 🔨 Сборка из исходного кода
#### 1️⃣ Установите необходимые пакеты:
```bash
apt install curl wget git docker docker-compose docker-buildx
```

#### 2️⃣ Клонируйте репозиторий:
```bash
git clone https://github.com/GnomeShift/TiSk && cd TiSk
```

#### 3️⃣ Соберите фронтенд:
```bash
docker buildx build -t tisk-frontend -f Dockerfile-frontend .
```

#### 4️⃣ Соберите бэкенд:
```bash
docker buildx build -t tisk-backend -f Dockerfile-backend .
```

# ⚙️ Конфигурация
#### 1️⃣ Сгенерируйте секрет:
```bash
sed -i "s/^JWT_AUTH_SECRET=.*/JWT_AUTH_SECRET=$(openssl rand -hex 64)/" .env.production
```

#### 2️⃣ Смените пароль Postgres:
```bash
sed -i "s/^DB_PASSWORD=.*/DB_PASSWORD=$(openssl rand -hex 24)/" .env.production
```

#### 3️⃣ Измените значение `addr=` на свой IP-адрес или домен:
```bash
addr= && sed -i "s/^API_URL=.*/API_URL=http:\/\/$addr\/api/" .env.production && sed -i "s/^FRONTEND_DOMAIN=.*/FRONTEND_DOMAIN=http:\/\/$addr" .env.production
```

#### 4️⃣ Запустите компоненты системы:
```bash
docker-compose --env-file .env.production up -d
```

#### 5️⃣ Проверьте работоспособность системы:
Откройте браузер и перейдите на страницу по адресу, установленному в 3️⃣.

# 👁️ Демо-режим
Система имеет демо-режим, позволяющий опробовать все функции до начала полноценной работы.\
Активировать его можно командой
```bash
sed -i "s/^APP_SEED_DEMO_DATA=.*/APP_SEED_DEMO_DATA=true/" .env.production
```

> [!INFORMATION]
> Если в тикет-системе уже содержатся какие-либо данные, либо Вы хотите начать сначала, то выполните команду ниже
> в дополнение к предыдущей.

```bash
sed -i "s/^APP_SEED_DEMO_DATA_FORCE=.*/APP_SEED_DEMO_DATA_FORCE=true/" .env.production
```

> [!WARNING]
> Запуск системы с `APP_SEED_DEMO_DATA_FORCE=true` УНИЧТОЖИТ ВСЕ ДАННЫЕ!!!

После этого тикет-система при запуске заполнит базу данных демонстрационными данными.
Для входа используйте данные из таблицы ниже.

| Роль          | Email                | Пароль     |
|---------------|----------------------|------------|
| Администратор | admin@example.com    | Admin123   |
| Поддержка     | support@example.com  | Support123 |
| Поддержка     | support2@example.com | Support123 |
| Пользователь  | user@example.com     | User123    |
| Пользователь  | user2@example.com    | User123    |
| Пользователь  | user3@example.com    | User123    |
